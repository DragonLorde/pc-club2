import styled from "styled-components"

const Title = styled.p`
    z-index: 20;
    font-style: normal;
    font-weight: 400;
    font-size: 32px;
    line-height: 39px;

    color: #FFFFFF;
`

export default Title