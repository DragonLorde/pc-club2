import styled from 'styled-components'

const Wrapper = styled.div`
    display: flex;
    width: 100vw;
    overflow: hidden;
    background: #1A1A42;
    height: 100%;
`

const Container = styled.div`
    display: flex;
    width: 100vw;
    height: 100%;
`

export const Styled = {
    Wrapper,
    Container,
}